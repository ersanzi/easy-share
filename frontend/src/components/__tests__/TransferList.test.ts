import{mount}from'@vue/test-utils';import{describe,expect,it}from'vitest';import TransferList from'../TransferList.vue';describe('TransferList',()=>{it('renders progress and pending actions',()=>{const wrapper=mount(TransferList,{props:{tasks:[{id:'1',fileName:'demo.zip',direction:'receive',peer:'Office PC',totalBytes:100,transferredBytes:40,speed:0,status:'pending'}]}});expect(wrapper.text()).toContain('demo.zip');expect(wrapper.text()).toContain('40%');expect(wrapper.text()).toContain('接收')})})
