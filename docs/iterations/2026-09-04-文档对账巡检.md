# 迭代记录 — 文档对账巡检（2026-09-04，自动化）

> 触发：每日 14:00 自主迭代调度轮 A 分支（周五）。**安全门拦截**：工作树存在用户未提交的剪切板修复改动（progress.md、troubleshooting.md、internal/clipboard、plugins/clipboard、panel_windows.go 等），按执行契约本轮只做规划与文档更新、不改任何代码，故执行待办池 P3「文档对账巡检」（契约指定的被拦截时填充任务）。
> 流程说明：progress.md「进行中」登记因该文件在用户 WIP 中而跳过，以本文档 + tasks.md 运行日志代替登记；本任务为纯文档巡检，树干净后无需补登记。

## 检查项与方法

1. **断链检查**：README.md、docs/progress.md、docs/tasks.md、docs/known-issues.md、docs/troubleshooting.md 五个真相源的全部相对链接，按链接所在文件目录解析（首跑脚本曾误按仓库根解析产生大量误报，已修正脚本后重跑——该坑值得记入 troubleshooting：markdown 相对链接的校验必须以所在文件为基准）。
2. **状态一致性**：known-issues.md 登记表 ↔ progress.md「已知阻塞」↔ tasks.md 用户动作区三方对账；README 概览与环境要求抽查（Wails CLI 2.13.0 ↔ go.mod v2.13.0）。
3. **迭代闭环**：docs/iterations/ 文件清单 ↔ progress.md 迭代记录表 ↔ git log（`git show --stat` 逐提交核对）。

## 发现与处置

| # | 发现 | 定性 | 处置 |
| --- | --- | --- | --- |
| 1 | 五个真相源相对链接 0 断链 | ✅ 通过 | — |
| 2 | progress.md 迭代记录表缺「2026-09-02 Linux 生产服务器部署资产」行——该主题有迭代文档（2026-09-02-linux-server-deploy.md）、「进行中」段落与提交 51ebf0d，唯独缺表行（同日的剪切板修复行已在） | ⚠️ 漂移 | progress.md 在用户 WIP 中，不代改；已登记 tasks.md 待办池 P3（树干净后补） |
| 3 | tasks.md 用户动作区称「Linux 部署资产仍在工作树」，实际 51ebf0d 已于 09-02 提交（工作树仅剩剪切板修复） | ⚠️ 过时 | 本轮直接勘误（tasks.md 为自动化队列文件，非用户代码 WIP；勘误仅改事实表述，未代做「提交」动作） |
| 4 | KI-6（LLM 限流时 /query 整体 500 不降级）状态「未修复」与 09-01 生产检索回灌无冲突——回灌做的是策略级降级（multi_hop 无 LLM 降级），非生成阶段运行时降级 | ✅ 无漂移 | — |
| 5 | README 基线描述、Wails CLI 版本与 progress.md / go.mod 一致 | ✅ 通过 | — |

## 规划结论（A 轮第 2 步：待办池重排）

- 待办池顺序**维持现状**：P1 contextual chunking 队首（2026-09-03 情报扫描获 Anthropic Contextual Retrieval 主流化佐证，执行信心上调）；P2 观察期周报脚本贴观察期关键路径，居次合理。
- 新增 1 条 P3（发现 #2 的表行补齐，树干净后即可做）。
- digest 中 3 条 P2 候选（reranker 候选池参数核对、评测集 hop 类型标注列、Wails v2.14.0 升级评估）**未**入待办池——按来源约束（新想法等用户拍板）留在 [2026-09-03 digest](../intelligence/2026-09-03-digest.md) 等用户审阅。
- 树干净后的首个迭代轮建议：按池序做 P1 contextual chunking，且只做切片一（标题路径/文档摘要前缀注入 + 42 条评测集对比不回退），符合「大任务只做一片」契约。

## 验证

纯文档轮，无需代码回归。本轮改动仅两个文件：本文档（新建）与 docs/tasks.md（勘误 + 待办池 + 运行日志）；git 状态其余条目与轮前完全一致，用户 WIP 未触碰、未提交任何内容。
